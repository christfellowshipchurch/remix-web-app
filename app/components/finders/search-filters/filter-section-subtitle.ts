/** Shared with `FilterPopup` body section labels and class finder `AllFiltersFilterSection` (`expandAlways`). */
export const finderFilterSectionSubtitleClass =
  'uppercase font-semibold text-xs text-neutral-default tracking-[0.6px]';
