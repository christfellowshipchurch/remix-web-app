import { ImageGallery } from "../../types";

// TODO: Fix the type of data
export const ImageGalleryComponent = ({ data }: { data: ImageGallery }) => {
  return (
    <div>
      <h2>Image Gallery</h2>
    </div>
  );
};
